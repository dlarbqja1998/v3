# 골라바유 작업 지침

## Git 커밋

- Git 커밋은 반드시 사용자에게 사전 허락을 받은 뒤에만 수행한다.
- 커밋 허락을 요청할 때는 변경 내용을 아래 형식으로 짧게 제시한다.
  - `추가 : <추가한 기능 또는 파일>`
  - `수정 : <수정한 기능 또는 파일>`
- 사용자가 명시적으로 승인하기 전에는 스테이징과 커밋을 하지 않는다.

## 제품 맥락

- 목표: 고려대학교 세종캠퍼스 학생이 매일 켜는 생활 앱 `골라바유`를 만든다.

## 구현 규칙

- 모든 사용자 문구와 문서는 한국어로 작성한다.
- 파일명 등 식별 목적의 이름은 영어를 사용해도 된다.
- 골라바유는 현재 SvelteKit 웹앱이지만, 이후 네이티브 앱 WebView로 감싸 출시할 수 있음을 전제로 구현한다.
- 모바일 화면을 우선으로 설계하고, 안전 영역, 소프트 키보드, 뒤로 가기, 외부 링크, 파일·카메라·알림 같은 브라우저 기능은 앱 환경 호환성을 함께 검토한다.
- 브라우저 전용 API를 사용할 때는 지원 여부를 확인하고, 앱 환경에서 필요한 대체 동작이나 실패 상태를 제공한다.
- 기획문서를 만들려고 할 때, md파일을 v3/md/planning폴더에 저장한다. 제목 양식은 planning_

## GB1 스타일

- 정의: 카드보다 목록과 구분선, 버튼보다 선택 상태를 우선하는 골라바유의 미니멀 모바일 생활정보 UI다.
- 화면: 흰색 기반으로 구성하고, 상단 헤더와 하단 내비게이션은 고정한다. 헤더의 제목은 중앙 정렬한 18px 굵은 글자를 사용한다.
- 정보 위계: 모든 새 화면과 기존 화면 수정에 적용한다. 한 화면에는 첫 시선이 머무는 주 정보 하나를 두고, 인접한 서로 다른 정보는 크기·굵기·색·간격 중 최소 두 가지 이상을 달리한다. 기본 순서는 페이지 제목(18px 굵게) → 현재 맥락(날짜·선택 장소 등 16px 굵게) → 섹션 제목(15px 굵게) → 본문(13px) → 보조 동작(13px 안팎의 연한 회색)이다.
- 정보 위계: 같은 글자 크기와 대비로 날짜, 섹션, 메뉴처럼 역할이 다른 정보를 나란히 두지 않는다. 여백과 1px 구분선으로 읽는 순서를 보강하고, 장식용 카드로 위계를 대신하지 않는다.
- 선택: 탭은 텍스트 중심으로 구성한다. 기본 상태는 `brand-muted`, 선택 상태는 크림슨(`brand`)과 굵은 글자이며, 2px 밑줄이 200~300ms 동안 이동한다.
- 정보: 콘텐츠는 넓은 카드 컨테이너 대신 1px 구분선과 48~56px 높이의 행으로 나눈다. 모서리와 그림자는 필요한 작은 보조 요소에만 10~14px 수준으로 제한한다.
- 동작: 보조 동작은 13px 안팎의 연한 회색 텍스트와 작은 아이콘으로 두고, 한 화면의 주요 행동만 크림슨 채움 버튼으로 강조한다. 모션은 짧고 절제되게 사용한다.

## 보고 방식

- 작업을 시작할 때는 대상과 접근 방법을 한두 문장으로 알린다.
- 작업 중에는 30초 이상 걸리는 조사, 구현, 검증의 진행 상황과 발견한 중요한 사실만 짧게 공유한다.
- 외부 상태를 바꾸는 작업, 범위가 달라지는 결정, 반복되는 오류는 근거와 함께 즉시 보고하고 사용자 지시를 기다린다.
- 작업 완료 시에는 변경 사항, 실제 실행한 검증 결과, 남은 사용자 작업만 짧게 보고한다.
- 검증하지 못한 항목은 완료로 표현하지 않고, 이유와 필요한 다음 조치를 명확히 알린다.
